# PairUp 环境配置文件
# 生产环境中请更改这些值

# 服务器配置
PORT=3000
NODE_ENV=development

# JWT配置 - 生产环境中请使用强密码
JWT_SECRET=pairup_super_secret_jwt_key_change_in_production_2024

# 数据库配置
DB_PATH=./database/pairup.db

# 安全配置
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=7d

# CORS配置
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Session Secret - 生产环境中请使用强密码
SESSION_SECRET=pairup_session_secret_change_in_production_2024

# Google OAuth配置
# 获取这些凭据需要在 https://console.developers.google.com/ 创建项目
# 1. 创建项目或选择现有项目
# 2. 启用 Google+ API 和 Google People API
# 3. 创建OAuth 2.0凭据
# 4. 添加重定向URI: http://localhost:3000/auth/google/callback
GOOGLE_CLIENT_ID=391365837056-orpev6lf4ia10eir5vggrpa9bdjpc9l3.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google OAuth callback URL - try different options if redirect_uri_mismatch occurs
# Common alternatives: /oauth2callback, /auth/google/callback, /callback
GOOGLE_CALLBACK_URL=http://localhost:3000/oauth2callback

# 邮件配置 (用于邮箱验证，可选)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password